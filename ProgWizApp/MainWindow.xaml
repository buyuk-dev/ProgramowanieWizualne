<Window x:Class="Michalski.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Michalski"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="521">

    <TabControl>
        <TabItem Header="Violins">
            <DataGrid x:Name="ViolinsDG" ItemsSource="{Binding}">
            </DataGrid>
        </TabItem>
        <TabItem Header="Makers">
            <DataGrid x:Name="MakersDG" ItemsSource="{Binding}">
            </DataGrid>
        </TabItem>
    </TabControl>
</Window>
